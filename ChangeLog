# Legacy ChangeLog

The canonical changelog is now maintained in GitHub-compatible Markdown at `CHANGELOG.md`.

This compatibility file is kept for Autotools/GNU distribution workflows and scripts that still expect a file named `ChangeLog`.
