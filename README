# Legacy README

The canonical project README is now maintained in GitHub-compatible Markdown at `README.md`.

This compatibility file is kept for Autotools/GNU distribution workflows and scripts or documentation that still expect a file named `README`.
